---
###############################################################
#                         Users Database                      #
###############################################################

# This file can be used if you do not have an LDAP set up.

# List of users
users:
  {{ username }}:
    displayname: "{{ name }}"
    password: "{{ password_hash }}"
    email: {{ email }}
    groups:
      - admins
      - dev
  john:
    displayname: john
    email: john@doe.com
    password: "{{ john_password_hash }}"
    groups:
      - admins
      - dev
...