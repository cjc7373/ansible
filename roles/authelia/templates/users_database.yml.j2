---
###############################################################
#                         Users Database                      #
###############################################################

# This file can be used if you do not have an LDAP set up.

# List of users
users:
  {{ username }}:
    displayname: "{{ name }}"
    password: "{{ password_hash }}"  # yamllint disable-line rule:line-length
    email: {{ email }}
    groups:
      - admins
      - dev
...