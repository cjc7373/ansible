apiVersion: 1

providers:
  - name: dashboards
    type: file
    updateIntervalSeconds: 30
    allowUiUpdates: true # make sure to save the updated dashboards back to ansible repo!
    options:
      path: /etc/grafana/dashboards
      foldersFromFilesStructure: true
